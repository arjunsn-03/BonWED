import OptionsPage from "@/components/OptionsPage";
export default function EngagementVendors() {
  return <OptionsPage eventType="engagement" category="vendor" title="Engagement Vendors & Contacts" emptyMessage="No vendors added yet — tap + to add one" />;
}
