import OptionsPage from "@/components/OptionsPage";
export default function EngagementClothing() {
  return <OptionsPage eventType="engagement" category="clothing" title="Engagement Clothing" emptyMessage="No clothing options added yet — tap + to add one" />;
}
