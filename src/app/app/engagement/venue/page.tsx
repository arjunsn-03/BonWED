import OptionsPage from "@/components/OptionsPage";
export default function EngagementVenue() {
  return <OptionsPage eventType="engagement" category="venue" title="Engagement Venues" emptyMessage="No venues added yet — tap + to add one" />;
}
