import OptionsPage from "@/components/OptionsPage";
export default function EngagementMakeup() {
  return <OptionsPage eventType="engagement" category="makeup" title="Engagement Makeup" emptyMessage="No makeup looks added yet — tap + to add one" />;
}
