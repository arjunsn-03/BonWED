import OptionsPage from "@/components/OptionsPage";
export default function EngagementInvitations() {
  return <OptionsPage eventType="engagement" category="invitations" title="Engagement Invitations" emptyMessage="No invitation designs added yet — tap + to add one" />;
}
