import OptionsPage from "@/components/OptionsPage";
export default function EngagementAccommodation() {
  return <OptionsPage eventType="engagement" category="accommodation" title="Engagement Accommodation" emptyMessage="No accommodation options added yet — tap + to add one" />;
}
