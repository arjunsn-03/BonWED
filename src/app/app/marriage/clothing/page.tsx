import OptionsPage from "@/components/OptionsPage";
export default function MarriageClothing() {
  return <OptionsPage eventType="marriage" category="clothing" title="Marriage Clothing" emptyMessage="No clothing options added yet — tap + to add one" />;
}
