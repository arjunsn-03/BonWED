import OptionsPage from "@/components/OptionsPage";
export default function MarriageVenue() {
  return <OptionsPage eventType="marriage" category="venue" title="Marriage Venues" emptyMessage="No venues added yet — tap + to add one" />;
}
