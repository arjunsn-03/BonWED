import OptionsPage from "@/components/OptionsPage";
export default function MarriageInvitations() {
  return <OptionsPage eventType="marriage" category="invitations" title="Marriage Invitations" emptyMessage="No invitation designs added yet — tap + to add one" />;
}
