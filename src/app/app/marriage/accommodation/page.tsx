import OptionsPage from "@/components/OptionsPage";
export default function MarriageAccommodation() {
  return <OptionsPage eventType="marriage" category="accommodation" title="Marriage Accommodation" emptyMessage="No accommodation options added yet — tap + to add one" />;
}
