import OptionsPage from "@/components/OptionsPage";
export default function MarriageMakeup() {
  return <OptionsPage eventType="marriage" category="makeup" title="Marriage Makeup" emptyMessage="No makeup looks added yet — tap + to add one" />;
}
