import OptionsPage from "@/components/OptionsPage";
export default function MarriageVendors() {
  return <OptionsPage eventType="marriage" category="vendor" title="Marriage Vendors & Contacts" emptyMessage="No vendors added yet — tap + to add one" />;
}
